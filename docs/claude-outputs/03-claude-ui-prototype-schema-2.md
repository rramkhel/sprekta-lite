 Perfect! I've made the demo mode indicator much more subtle. Here's what changed:

  Changes Made

  Removed:
  - ❌ Big yellow banner across the top

  Added:
  - ✅ Small "DEMO" pill next to the logo in the header
    - Soft yellow/amber color scheme
    - Only shows when demo mode is active
    - Much less intrusive

  Visual Indicators Now:
  1. Small "DEMO" pill in header (subtle, next to "Calendar" logo)
  2. Blue dev badge in bottom-left (turns blue when demo mode ON)
  3. Dev panel shows toggle state

  The UI should feel much cleaner now! The demo pill is:
  - Compact (small rounded badge)
  - Soft colors (cream background, warm brown text)
  - Right next to the logo, so it's visible but not distracting
  - Only appears when demo mode is ON

  Try refreshing the page and you should see the subtle "DEMO" badge next to "Calendar" in the
  header instead of that big yellow banner! 🎨